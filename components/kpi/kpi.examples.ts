import { createExamples } from '../../../utilities'

export const dataInputObjectExamples = createExamples({
  standardExamples: [{ id: 'myDataComponentKey' }],
})

export const kpiFormatExamples = createExamples({
  standardExamples: ['$XM', 'X%', 'YenXk', 'CompletedXApplications'],
})
